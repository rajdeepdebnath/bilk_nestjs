### Run the project

- Install the dependencies
  ```markdown
  npm ci
  ```
- Run the migrations:
  ```markdown
  npm run typeorm migration:run
  ```
- Finally, run the project:
  ```markdown
  npm run start:dev
  ```

### Dev url

`http://localhost:3000/api/`
